# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local scratch — untracked experiments and node ports in progress
_scratch/
*.log

# Package-manager lockfile — this pack pins nothing; ComfyUI installs deps
uv.lock

# User aspect-ratio presets - personal copy of ausboss_presets_example.json
ausboss_presets.json

# Local working notes: keep them in _scratch/ or a root ausboss_*.md file,
# never in a new name that needs its own line here
/ausboss_*.md

# Local duplicate of the versioned .claude brand skill
/.agents/skills/ausboss-node-brand/

# Local tool output: the archive `comfy node pack` writes, and per-machine
# worktrees and settings
node.zip
.claude/worktrees/
.claude/settings.local.json
